<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:id="@+id/hourLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Hour"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"/>
    <ImageButton
        android:id="@+id/endHourUpBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/hourLabel"
        android:src="@drawable/ic_arrow_drop_up_black_24dp"/>

    <TextView
        android:id="@+id/endHourTimerText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/endHourUpBtn"
        app:layout_constraintBottom_toTopOf="@id/endHourDownBtn"
        android:gravity="center"
        android:textSize="30sp"
        android:text="--" />

    <ImageButton
        android:id="@+id/endHourDownBtn"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_arrow_drop_down_black_24dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>