<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:id="@+id/minuteLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Minute"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"/>
    <ImageButton
        android:id="@+id/endMinUpBtn"
        app:layout_constraintTop_toBottomOf="@id/minuteLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_arrow_drop_up_black_24dp"/>

    <TextView
        android:id="@+id/endMinTimerText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/endMinUpBtn"
        app:layout_constraintBottom_toTopOf="@id/endMinDownBtn"
        android:gravity="center"
        android:textSize="30sp"
        android:text="--" />

    <ImageButton
        android:id="@+id/endMinDownBtn"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_arrow_drop_down_black_24dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>